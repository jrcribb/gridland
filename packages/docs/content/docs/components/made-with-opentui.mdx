---
title: Made with OpenTUI
description: Branding components that link back to the OpenTUI project
---

import MadeWithOpentuiDemo from '@/components/demos/made-with-opentui-demo'

The `CornerRibbon`, `BadgeButton`, and `TextBadge` components from `@gridland/ui` provide
branding attribution that links back to the OpenTUI project website.

<div className="my-8">
  <MadeWithOpentuiDemo />
</div>

## Installation

```bash
bunx shadcn@latest add https://raw.githubusercontent.com/cjroth/gridland/main/packages/ui/registry.json made-with-opentui
```

## Import

```tsx title="Import"
import { CornerRibbon, BadgeButton, TextBadge } from "@/components/ui/made-with-opentui"
```

## Usage

```tsx title="CornerRibbon"
<CornerRibbon position="top-right" />
```

```tsx title="BadgeButton"
<BadgeButton variant="dark" />
```

```tsx title="TextBadge"
<TextBadge />
```

## CornerRibbon Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `position` | `"top-right" \| "top-left" \| "bottom-right" \| "bottom-left"` | `"top-right"` | Corner position |
| `className` | `string` | `undefined` | Additional CSS classes on the ribbon link |
| `absolute` | `boolean` | `false` | Use absolute instead of fixed positioning |

## BadgeButton Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `variant` | `"dark" \| "light" \| "outline"` | `"dark"` | Visual style variant |
| `className` | `string` | `undefined` | Additional CSS classes |

## TextBadge Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `className` | `string` | `undefined` | Additional CSS classes |
