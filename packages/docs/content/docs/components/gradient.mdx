---
title: Gradient
description: Apply color gradients to text
---

import { Tabs, Tab } from 'fumadocs-ui/components/tabs'
import { RunDemo } from '@/components/ui/run-demo'
import GradientDemo from '@/components/demos/gradient-demo'

The `Gradient` component from `@gridland/ui` applies color gradients across text characters.

<div className="my-8">
  <GradientDemo />
</div>

<RunDemo name="gradient" />

## Installation

<Tabs items={['bun', 'npm', 'yarn', 'pnpm']}>
<Tab value="bun">
```bash title="Terminal"
bun add @gridland/ui
```
</Tab>
<Tab value="npm">
```bash title="Terminal"
npm install @gridland/ui
```
</Tab>
<Tab value="yarn">
```bash title="Terminal"
yarn add @gridland/ui
```
</Tab>
<Tab value="pnpm">
```bash title="Terminal"
pnpm add @gridland/ui
```
</Tab>
</Tabs>

## Usage

```tsx title="Import"
import { Gradient } from "@gridland/ui"
```

### Named Gradient

```tsx title="Named gradient"
<Gradient name="rainbow">Hello World!</Gradient>
```

### Custom Colors

```tsx title="Custom colors"
<Gradient colors={["#ff6b6b", "#feca57", "#48dbfb"]}>Custom gradient</Gradient>
```

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `children` | `string` | - | Text content to apply the gradient to |
| `name` | `GradientName` | - | Named gradient preset (see list below) |
| `colors` | `string[]` | - | Array of hex colors for custom gradient |

> **Note:** `name` and `colors` are mutually exclusive. Provide exactly one.

## Available Gradient Names

`cristal`, `teen`, `mind`, `morning`, `vice`, `passion`, `fruit`, `instagram`, `atlas`, `retro`, `summer`, `pastel`, `rainbow`
