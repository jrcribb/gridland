---
title: Tabs
description: Compound tab component with triggers and content panels
---

import { RunDemo } from '@/components/ui/run-demo'
import TabsDemo, { TabsSimpleDemo } from '@/components/demos/tabs-demo'

A compound tab component inspired by shadcn/ui. Declare triggers and content panels
side by side. The active panel renders automatically.

<div className="my-8">
  <TabsSimpleDemo />
</div>

## Example

<div className="my-8">
  <TabsDemo />
</div>

<RunDemo name="tabs" />

## Installation

```bash
bunx shadcn@latest add https://raw.githubusercontent.com/cjroth/gridland/main/packages/ui/registry.json tab-bar
```

## Usage

```tsx title="Compound API"
import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/ui/tab-bar"

<Tabs defaultValue="files">
  <TabsList>
    <TabsTrigger value="files">Files</TabsTrigger>
    <TabsTrigger value="search">Search</TabsTrigger>
    <TabsTrigger value="git">Git</TabsTrigger>
  </TabsList>
  <TabsContent value="files">
    <text>Browse project files</text>
  </TabsContent>
  <TabsContent value="search">
    <text>Search across files</text>
  </TabsContent>
  <TabsContent value="git">
    <text>View git status</text>
  </TabsContent>
</Tabs>
```

```tsx title="Simple API"
import { TabBar } from "@/components/ui/tab-bar"

<TabBar label="View" options={["Files", "Search", "Git"]} selectedIndex={0} />
```

## Components

### Tabs

Root component that manages the active tab state.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `string` | - | Controlled active tab value |
| `defaultValue` | `string` | `""` | Default active tab (uncontrolled) |
| `onValueChange` | `(value: string) => void` | - | Called when the active tab changes |

### TabsList

Renders the horizontal tab bar from its `TabsTrigger` children.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `label` | `string` | - | Text label shown before the triggers |
| `focused` | `boolean` | `true` | Whether the tab bar appears focused |
| `activeColor` | `string` | `theme.accent` | Foreground color of the active trigger |
| `separator` | `boolean` | `true` | Show horizontal separator below triggers |

### TabsTrigger

Declares a tab option. Does not render on its own. `TabsList` reads its props.

| Prop | Type | Description |
|------|------|-------------|
| `value` | `string` | Unique value linking this trigger to its `TabsContent` |
| `children` | `ReactNode` | Label text displayed in the tab bar |

### TabsContent

Renders its children only when its `value` matches the active tab.

| Prop | Type | Description |
|------|------|-------------|
| `value` | `string` | Must match the active tab value to render |
| `children` | `ReactNode` | Content shown when this tab is active |

### TabBar (simple API)

A convenience wrapper for cases where you don't need content panels.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `label` | `string` | - | Text label shown before the options |
| `options` | `string[]` | - | Array of option strings to display |
| `selectedIndex` | `number` | - | Zero-based index of the selected option |
| `focused` | `boolean` | `true` | Whether the tab bar appears focused |
| `activeColor` | `string` | `theme.accent` | Foreground color of the selected option |
| `separator` | `boolean` | `true` | Show horizontal separator below tabs |
